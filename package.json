{
  "name": "drvn-mini-app-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "DRVN VHCLS Mini App - Frontend and Smart Contracts",
  "scripts": {
    "prepare": "husky install",
    "postinstall": "cd Frontend && npm install",
    "build": "cd Frontend && npm run build",
    "start": "cd Frontend && npm start"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}

